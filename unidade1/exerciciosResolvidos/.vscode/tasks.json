{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "RodarEntrada",
            "type": "shell",
            "command": "javac ${input:fileName}.java && java ${input:fileName} < ${input:entrada}",
            "args":["${input:entrada}"]
        }
    ],
    "inputs": [
        {
            "id": "fileName",
            "description": "Arquivo .java:",
            "default": "Aula",
            "type": "promptString"
        },
        {
            "id": "entrada",
            "description": "Arquivo com extensão:",
            "default": "entrada.txt",
            "type": "promptString"
        },
    ]
    }